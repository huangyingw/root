#!/bin/bash
. $HOME/.envrc
. $HOME/.shellrc
. $HOME/.gitrc
. $HOME/.kvmrc
. $HOME/openrc.sh
. $HOME/.hgrc
. $HOME/.sqlrc
. $HOME/.p4rc
HNAME=`hostname`
if [ $HNAME == "SCL-1YIHUANG-M.local" ]
then
  . $HOME/.rovirc
fi
