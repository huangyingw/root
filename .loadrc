#!/bin/bash
. $HOME/.shellrc
. $HOME/.envrc
. $HOME/.gitrc
. $HOME/.kvmrc
. $HOME/openrc.sh
. $HOME/.hgrc
. $HOME/.sqlrc
HNAME=`hostname`
if [ $HNAME == "huangyingw-uuid.local" ]
then
  . $HOME/.xsrc
fi
