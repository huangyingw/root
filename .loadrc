#!/bin/bash
. $HOME/.vimrc
. $HOME/.envrc
. $HOME/.shellrc
. $HOME/.gitrc
. $HOME/.kvmrc
. $HOME/openrc.sh
. $HOME/.hgrc
. $HOME/.sqlrc
. $HOME/.p4rc
HNAME=`hostname`
if [ $HNAME == "huangyingw-uuid.local" ]
then
  . $HOME/.xsrc
fi
