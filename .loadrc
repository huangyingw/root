#!/bin/bash
. $HOME/.envrc
. $HOME/.shellrc
. $HOME/.gitrc
. $HOME/.kvmrc
. $HOME/openrc.sh
. $HOME/.hgrc
. $HOME/.sqlrc
. $HOME/.p4rc
. $HOME/.svnrc
. $HOME/.androidrc

HNAME=`hostname`
if [ $HNAME == "scl-1yihuang-m.corporate.local" ]
then
  . $HOME/.rovirc
fi
